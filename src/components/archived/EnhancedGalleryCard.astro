---
// Archived copy of EnhancedGalleryCard. Original moved to archived.
import OptimizedGalleryImage from "../OptimizedGalleryImage.astro";
const { project } = Astro.props;

if (!project) return;

const { data } = project;
const title = data.title || '';
---

<div class="gallery-card-archived">
  <h3>{title}</h3>
</div>
